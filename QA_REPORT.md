# SKACK Overflow 기능 QA

## 재검증 결과

| 구분 | 결과 | 확인 범위 |
| --- | --- | --- |
| 핵심 익명 Q&A | 통과 | UID 생성, 질문, 검색, 필터, 답변, 댓글·답글, 공감, 채택 권한, 새로고침 복원 |
| 입력·안전 경계 | 통과 | 빈 입력, 입력 길이, 로컬 신고 기록, 사용자 차단 |
| 데이터 관리 | 통과 | 브라우저별 기록 JSON 내보내기와 초기화 |
| 모달 접근성 | 통과 | Escape, 오버레이 클릭 닫기, Tab 포커스 순환 |
| 내 콘텐츠 관리 | 통과 | 질문·답변·댓글 수정과 삭제 |
| 코드 품질 | 통과 | TypeScript 검사와 프로덕션 빌드 |

## 구현된 보완

QA에서 확인된 P0 모달 이탈 문제는 Escape와 오버레이 클릭 닫기, 모달 내부 Tab 포커스 순환으로 보완했습니다. P1의 콘텐츠 관리 문제는 작성자 UID를 기준으로 질문·답변·댓글의 수정·삭제를 분리했습니다. 비작성자에게는 로컬 신고 기록과 차단 동작을 제공하며, 차단 뒤에는 해당 사용자 질문·답변·댓글을 즉시 화면에서 제외합니다.

## 1차 반복 QA: 재현 결과

| 발견 항목 | 결과 | 보완 우선순위 |
| --- | --- | --- |
| 질문 공감 | 상단 공감 버튼이 표시만 되고 상태를 변경하지 않습니다. | P0 |
| 조회 수 | 상세 진입 뒤 조회 수가 로컬 저장 목록에 반영되지 않습니다. | P0 |
| 상세 재진입 | `#q-질문ID` 링크로 해당 질문을 바로 열 수 없습니다. | P1 |

## 1차 리뷰 결정

질문 공감과 조회 수 저장은 피드의 신뢰 지표가 실제 행동과 일치하지 않는 문제이므로 P0로 즉시 보완합니다. 해시 링크는 공유와 재방문에 직접 연결되므로 P1로 같은 차수에 반영합니다. 모두 브라우저 로컬 저장 범위에서 해결 가능하며 서버 기능 추가 없이 기존 익명 UID 모델을 유지합니다.

## 1차 보완 검증

질문 공감은 브라우저별 중복 방지 상태와 함께 저장되며, 조회 수는 상세 진입 시 질문 목록과 로컬 저장 상태에 반영됩니다. `#q-질문ID` 해시는 해당 질문의 상세 화면을 직접 열고, 닫을 때 해시를 정리합니다. 세 항목은 재검증에서 모두 통과했습니다.

## 2차 반복 QA: 재현 결과

| 발견 항목 | 결과 | 보완 우선순위 |
| --- | --- | --- |
| 모달 초기 포커스 | 상세 모달을 열어도 현재 포커스가 목록 버튼에 남습니다. | P0 |
| 브라우저 뒤로가기 | 주소는 이전 상태로 바뀌지만 상세 모달이 남습니다. | P1 |

## 2차 보완 검증

상세 모달의 닫기 버튼은 열리는 즉시 포커스를 받습니다. 브라우저 뒤로가기는 `popstate`를 통해 해시와 상세 상태를 동기화해 모달을 닫습니다. 1차 QA의 공감·조회·해시 항목을 포함한 재검증에서 모두 통과했습니다.

## 3차 반복 QA: 재현 결과

해시로 상세 화면에 재진입하는 기능은 통과했지만, 사용자가 해당 링크를 직접 복사할 제어가 없습니다. 공유 재진입의 마지막 행동이 누락된 P2 과제로 분류해 상세 화면에 최소 제어를 추가합니다.

## 3차 보완 검증

상세 화면에 링크 복사 제어를 추가했고, 클릭 뒤 사용자 피드백을 확인했습니다. 1·2차의 공감·조회 저장·해시 진입·포커스·뒤로가기 항목과 함께 재검증에서 모두 통과했습니다.

## 1차 기준 회귀 대조

| 검증군 | 1차 시작 시점 | 최종 대조 |
| --- | --- | --- |
| 기존 핵심 흐름 15개 | 통과 | 통과 |
| 경계·권한·데이터 흐름 16개 | 통과 | 통과 |
| 1차 발견 3개 | 공감·조회·해시 결함 | 통과 |
| 2차 발견 2개 | 모달 초기 포커스·뒤로가기 결함 | 통과 |
| 3차 발견 1개 | 링크 복사 제어 부재 | 통과 |

1차 기준으로 돌아가 TypeScript 검사, 프로덕션 빌드, 총 39개 자동 검증을 다시 실행했습니다. 기존 통과 항목은 유지됐고, 세 차수에서 발견된 6개 항목은 모두 재현·보완·재검증을 마쳤습니다.

## 새 기준 1차 반복 QA: 재현 결과

브라우저 기록을 JSON으로 내보낼 수 있지만 다시 가져올 파일 제어가 없습니다. 같은 익명 UID 모델을 유지하면서 학습 기록을 옮길 수 없으므로 P2 데이터 이관 과제로 보완합니다.

## 새 기준 1차 보완 검증

데이터 도구에 JSON 가져오기를 추가했습니다. 질문 배열·식별자·필수 필드를 검증한 뒤 유효한 기록만 반영하며, 익명 UID와 차단 목록도 함께 복원합니다. 유효한 JSON을 사용한 질문 목록 반영과 UID 복원 검증을 통과했습니다.

## 새 기준 2차 반복 QA: 재현 결과

내 질문에 새 답변이 도착하면 알림은 보이지만, 질문 상세를 열어 확인한 뒤에도 읽음 상태가 없어 계속 남습니다. 답변을 확인한 행동이 알림과 연결되지 않는 P2 흐름 결함으로 분류합니다.

## 새 기준 2차 보완 검증

질문 작성자가 상세를 열면 외부 답변 수를 브라우저별 읽음 상태로 저장하고, 새 답변 수가 읽음 수를 초과할 때만 알림을 보이도록 변경했습니다. 답변 확인 뒤 새 답변 알림 해제 검증을 통과했습니다.

## 새 기준 3차 반복 QA: 재현 결과

읽음 상태를 추가한 뒤 내보낸 JSON에는 해당 상태가 포함되지 않아, 다른 브라우저로 가져오면 새 답변 알림 맥락이 사라집니다. 데이터 이관 완전성을 위해 P2로 보완합니다.

## 새 기준 3차 보완 검증

내보내기 JSON에 읽음 상태를 포함하고, 가져오기 때 숫자 값만 검증해 복원하도록 변경했습니다. 가져오기→내보내기→재가져오기 순서에서 읽음 상태가 유지되는지 검증해 통과했습니다.

## 1차 지시문 재적용과 회귀 대조

| 검증군 | 결과 |
| --- | --- |
| 기존 핵심·경계·권한 흐름 39개 | 통과 |
| 새 기준 1차 데이터 가져오기 3개 | 통과 |
| 새 기준 2차 답변 읽음 처리 1개 | 통과 |
| 새 기준 3차 읽음 상태 이관 2개 | 통과 |

1차 지시문을 다시 적용해 총 45개 자동 검증, TypeScript 검사, 프로덕션 빌드를 실행했습니다. 이전 통과 항목의 회귀는 없었고, 새로 발견된 데이터 이관·읽음 상태 관련 3개 결함도 모두 재현·보완·재검증했습니다.

## 추가 반복 1차 QA: 재현 결과

가져오기 JSON은 질문의 최상위 필드만 확인해, 답변·댓글 내부 구조가 깨진 파일도 통과합니다. 이후 상세 화면에서 배열 메서드를 호출할 때 화면이 정상적으로 이어지지 않는 P1 데이터 무결성 결함으로 분류합니다.

## 추가 반복 1차 보완 검증

가져오기 전에 질문·답변·댓글의 필수 필드와 배열 구조를 전체 검증하도록 보완했습니다. 중첩 답변 구조가 깨진 JSON은 질문 목록에 반영하지 않고 안내를 표시하며, 상세 화면 오류 없이 거부되는 것을 확인했습니다.

## 추가 반복 2차 QA: 재현 결과

유효한 JSON을 선택하면 기존 브라우저 기록을 즉시 교체합니다. 내보내기·가져오기는 데이터 손실 위험이 있는 행동이므로, 가져오기 전 사용자 확인이 필요한 P1 안전성 결함으로 분류합니다.

## 추가 반복 2차 보완 검증

유효한 JSON을 읽은 뒤 질문 수를 안내하는 교체 확인 대화상자를 표시하도록 변경했습니다. 사용자가 승인한 경우에만 질문 목록을 반영하는 흐름을 검증해 통과했습니다.

## 추가 반복 3차 QA: 재현 결과

가져온 기록에 선택 항목인 차단 목록이나 읽음 상태가 없으면, 이전 브라우저의 값이 그대로 남습니다. 기록 교체 뒤 이전 사용자 설정이 새 기록에 섞이는 P1 상태 격리 결함으로 분류합니다.

## 추가 반복 3차 보완 검증

가져오기에서 차단 목록과 읽음 상태가 없으면 해당 상태를 빈 값으로 초기화하도록 변경했습니다. 이전 차단 UID가 새 기록의 답변을 숨기지 않는지 검증해 통과했습니다.

## 추가 반복 1차 지시문 재적용과 회귀 대조

| 검증군 | 결과 |
| --- | --- |
| 기존 핵심·경계·데이터 흐름 45개 | 통과 |
| 중첩 JSON 구조 거부 1개 | 통과 |
| 기록 교체 확인·승인 반영 2개 | 통과 |
| 누락 상태 초기화 1개 | 통과 |

1차 지시문을 다시 적용해 총 49개 자동 검증, TypeScript 검사, 프로덕션 빌드를 실행했습니다. 기존 통과 항목은 유지됐고, 추가 반복에서 발견된 데이터 무결성·교체 확인·상태 격리 결함 3개를 모두 재현·보완·재검증했습니다.

## 후속 반복 1차 QA: 재현 결과

상세 화면에서 질문을 삭제하면 모달은 닫히지만 `#q-질문ID` 해시가 주소에 남습니다. 삭제된 질문을 가리키는 주소 상태가 남는 P2 탐색 일관성 결함으로 분류합니다.

## 후속 반복 1차 보완 검증

질문 삭제 뒤 상세 상태를 닫는 공통 경로를 사용해 모달과 해시 주소를 함께 정리하도록 변경했습니다. 삭제 뒤 주소에 질문 해시가 남지 않는지 검증해 통과했습니다.

## 후속 반복 2차 QA: 재현 결과

질문 데이터만 다른 탭의 저장소 변경을 듣고, 차단·읽음 상태는 현재 탭에 남아 있습니다. 다른 탭에서 차단한 사용자의 답변이 그대로 보여 익명 안전장치가 탭마다 달라지는 P1 동기화 결함으로 분류합니다.

## 후속 반복 2차 보완 검증

저장소 변경 수신 범위에 차단 UID와 읽음 상태를 추가했습니다. 다른 탭에서 차단한 사용자의 답변이 현재 탭 상세 화면에서도 즉시 숨겨지는지 검증해 통과했습니다.

## 후속 반복 3차 QA: 재현 결과

다른 탭에서 기록을 가져오며 익명 UID가 바뀌어도 현재 탭 상태는 이전 UID를 유지합니다. 이후 현재 탭에서 새 질문을 쓰면 이전 UID로 작성돼 소유자 권한이 탭마다 갈리는 P1 권한 동기화 결함으로 분류합니다.

## 후속 반복 3차 보완 검증

저장소 변경 수신 범위에 익명 UID를 추가했습니다. 다른 탭에서 기록을 가져온 뒤 현재 탭이 새 UID로 질문을 작성하는지 검증해 통과했습니다.

## 후속 반복 1차 지시문 재적용과 회귀 대조

| 검증군 | 결과 |
| --- | --- |
| 기존 핵심·경계·데이터 흐름 49개 | 통과 |
| 질문 삭제 뒤 해시 정리 1개 | 통과 |
| 탭 간 차단·읽음 상태 동기화 1개 | 통과 |
| 탭 간 익명 UID 권한 동기화 1개 | 통과 |

1차 지시문을 다시 적용해 총 52개 자동 검증, TypeScript 검사, 프로덕션 빌드를 실행했습니다. 기존 통과 항목의 회귀는 없었고, 후속 반복에서 발견된 탐색 해시·탭 간 안전 상태·권한 동기화 결함 3개를 모두 재현·보완·재검증했습니다.

## 필터 QA: 재현 결과와 보완

상단 탐색에는 힌트 도착·내 막힘이 없었고, 도움 필요는 단순 미채택 질문을 세어 답변이 있는 항목까지 포함했습니다. 상단과 본문 필터를 동일한 상태로 연결하고, 도움 필요를 **공개 답변이 0개인 미해결 질문**으로 정의했습니다. 상단·본문 탭 전환, 도움 필요 카운트·목록 수 일치, 힌트 도착, 익명 작성 뒤 내 막힘 노출을 검증해 통과했습니다.

## 남은 개선 과제

| 우선순위 | 과제 | 현 상태 |
| --- | --- | --- |
| P1 | 서버 저장 기반 신고 검토·게시물 제한 | 정적 프로젝트이므로 현재는 브라우저별 로컬 기록만 지원합니다. |

## 필터 이후 1차 반복 QA: 재현 결과와 보완

반복 QA 스크립트의 `finding` 함수는 결함 조건이 참일 때도 `GAP`만 출력하고 프로세스를 실패시키지 않았습니다. 따라서 자동 검증 명령은 실제 결함이 있어도 성공 종료할 수 있는 P1 검증 신뢰성 결함이었습니다.

`qa-cycle-*`, `qa-new-cycle-*`, `qa-additional-cycle-*`, `qa-followup-cycle-*`, `qa-filter-check.mjs`의 `finding` 함수를 결함 조건에서 예외를 던지도록 통일했습니다. 이제 결함이 재현되면 검증 명령이 실패하고, 결함이 없을 때만 `PASS · 결함 재현 없음`을 출력합니다.

보정 후 TypeScript 검사, 전체 브라우저 검증 스크립트, 프로덕션 빌드를 다시 실행했습니다. 질문·답변·댓글·답글·공감·채택·필터·데이터 이관·탭 동기화와 기존 반복 QA 항목에서 결함 재현 없이 통과했습니다.

## 필터 이후 2차 반복 QA: 시각 리뷰·보완·검증

독립 시각 검토에서 문제 스트림의 신호 타임라인은 일관되지만, 히어로·탐색·오른쪽 패널은 일반 Q&A 보드처럼 읽히며 답변 중심 용어가 남아 있다는 점을 확인했습니다. 사용자 제공 결합 마크는 최종 기준이므로 교체하지 않고 유지했습니다.

히어로에 `막힘 로그 / 01 · 익명 신호 기록` 메타정보와 신호 레일을 추가했습니다. 리스트와 사이드바의 사용자 노출 문구는 힌트·도움 신호·다음 관점으로 정리했고, 사이드바에는 상태 키·관찰 태그·아카이브 도구 레이블을 보강했습니다. 헤더 탐색도 끊긴 신호선과 점으로 연결해 상위 영역의 타임라인 언어를 맞췄습니다.

보완 뒤 TypeScript 검사, 브라우저 자동 검증 **58개**, 프로덕션 빌드를 실행해 모두 통과했습니다.

## 필터 이후 3차 반복 QA: 모바일·브랜드 회귀 검증

모바일 전체 화면에서 SKACK·Overflow 워드마크, 히어로의 아카이브 메타정보, 막힘 로그 스트림, 상태 키·관찰 태그·아카이브 도구가 한 흐름으로 표시되는지 확인했습니다. 좁은 화면에서 상단 탐색은 숨기고 핵심 작성 행동과 신호 로그를 우선해 가로 넘침 없이 표시됐습니다.

워드마크의 색상·글꼴·굵기·자간이 다시 갈라지는 회귀를 막기 위해 전용 브라우저 검증을 추가했습니다. SKACK과 Overflow가 동일한 `rgb(230, 56, 77)`, IBM Plex Mono, 700 굵기, 동일 자간을 쓰는지 4개 조건으로 확인합니다.

전용 브랜드 검증 4개와 기존 자동 검증을 합친 **62개** 검증, TypeScript 검사, 프로덕션 빌드를 모두 통과했습니다.

## 필터 이후 1차 지시 재적용과 회귀 대조

1차 기준으로 화면의 사용자 노출 문구를 다시 점검한 결과, 상세 화면·힌트 작성·완료 안내에 답변·채택 언어가 남아 있었습니다. 또한 작성 버튼이 헤더와 모달에 같은 이름으로 노출되면서, 범용 역할 선택자를 쓰던 일부 자동 검증이 둘 이상의 버튼을 찾아 실패했습니다.

상세 화면의 개수·상태·선택 제어·빈 상태·작성 제어를 힌트·다음 관점 언어로 정리했고, 완료 안내와 자기 공감·수정·삭제 안내도 같은 언어로 맞췄습니다. 모달 제출 검증은 `.ask-modal` 범위로 한정해 같은 이름의 헤더 행동과 충돌하지 않도록 보완했습니다.

1차 지시문을 재적용해 모든 브라우저 검증을 실패 즉시 중단하는 방식으로 실행했습니다. 브랜드 규칙 4개를 포함한 **62개** 자동 검증, TypeScript 검사, 프로덕션 빌드가 모두 통과했습니다.

## 자동 검증

```bash
pnpm check
node tests/qa-brand-check.mjs
node tests/interaction-check.mjs
node tests/qa-edge-check.mjs
node tests/qa-cycle-1.mjs
node tests/qa-cycle-2.mjs
node tests/qa-cycle-3.mjs
node tests/qa-new-cycle-1.mjs
node tests/qa-new-cycle-2.mjs
node tests/qa-new-cycle-3.mjs
node tests/qa-additional-cycle-1.mjs
node tests/qa-additional-cycle-2.mjs
node tests/qa-additional-cycle-3.mjs
node tests/qa-followup-cycle-1.mjs
node tests/qa-followup-cycle-2.mjs
node tests/qa-followup-cycle-3.mjs
pnpm build
```
