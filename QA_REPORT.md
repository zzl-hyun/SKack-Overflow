# SKACK Overflow QA 보고서

> 이 보고서는 기존 누적 기록을 제거한 뒤, 현재 프로젝트 상태를 대상으로 수행하는 반복 QA의 근거와 결과만 기록합니다.

## 검증 범위

익명 UID, 질문·힌트·댓글·답글, 공감·관점 선택 권한, 필터·검색, 링크 복사, 데이터 가져오기·내보내기, 탭 동기화, 접근성 피드백, 워드마크·색상 역할을 검증합니다.

## 1차 QA: 기능 검증과 시각 결함

TypeScript 검사, 프로덕션 빌드, 익명 UID·질문·힌트·댓글·답글·권한·필터·데이터 이관·탭 동기화·접근성·브랜드 규칙을 포함한 자동 검증을 실행했습니다. **67개** 자동 검증과 TypeScript 검사, 프로덕션 빌드가 통과했습니다.

독립 시각 검토에서는 피드의 신호 타임라인은 명확하지만 히어로 오른쪽이 비어 있어, 첫 화면이 실제 학습 로그보다 정적 소개 영역처럼 읽힌다는 P1 정보 위계 결함을 확인했습니다.

### 보완

히어로에 최근 힌트 도착 질문 두 개를 실제 데이터로 표시하는 관찰 기록 패널을 추가했습니다. 각 기록은 단계·힌트 수·작성 시점을 보여주며, 클릭하면 해당 막힘 상세를 엽니다. 모바일에서는 핵심 제목·집계를 우선하도록 관찰 패널을 숨깁니다.

보완 뒤 전체 자동 검증 **67개**, TypeScript 검사, 프로덕션 빌드를 다시 실행해 통과했습니다.

## 2차 QA: 차단 상태와 히어로 관찰 기록

히어로 관찰 기록은 힌트가 도착한 질문을 표시했지만, 작성자를 차단한 뒤에도 해당 작성자의 기록을 계속 표시했습니다. 피드와 상세에서는 차단이 반영되는데 히어로에서만 남아 개인정보 노출 제어가 일관되지 않은 P1 결함으로 분류했습니다.

### 보완

관찰 기록의 후보 질문을 만들 때 차단한 작성자를 제외했습니다. 전용 브라우저 검증은 히어로 기록 노출, 해당 막힘 상세 진입, 작성자 차단 후 기록 즉시 숨김을 확인합니다.

전체 자동 검증은 **70개**, TypeScript 검사와 프로덕션 빌드는 모두 통과했습니다.

## 3차 QA: 작성 행동의 화면·보조기술 언어 일치

목록의 작성 버튼은 화면에는 ‘막힘 남기기’로 표시되지만 접근 가능한 이름은 ‘질문하기’였습니다. 화면 독자와 보조기술 사용자가 서로 다른 행동 명칭을 접하는 P2 언어 일관성 결함으로 분류했습니다.

### 보완

목록 작성 버튼의 접근 가능한 이름을 ‘막힘 남기기’로 맞췄습니다. 경계 QA에는 목록 버튼의 접근 가능한 이름이 화면 문구와 일치하는지 확인하는 검증을 추가했습니다.

전체 자동 검증은 **71개**, TypeScript 검사와 프로덕션 빌드는 모두 통과했습니다.

## 1차 절차 재적용과 최종 대조

1차 QA 절차를 다시 적용해 익명 UID, 질문·힌트·댓글·답글, 공감·관점 선택 권한, 필터·검색, 링크 복사 성공·실패 경로, 데이터 가져오기·내보내기, 탭 동기화, 히어로 관찰 기록, 차단 동기화, 접근 가능한 이름, 색상 역할을 한 번에 대조했습니다.

실패 시 즉시 중단하는 방식으로 **71개** 자동 검증, TypeScript 검사, 프로덕션 빌드를 실행해 모두 통과했습니다. 모바일 화면에서도 관찰 패널은 숨기고 신호 집계·막힘 로그를 우선해 가로 넘침 없이 표시되는 것을 확인했습니다.

## 답글 입력 포커스 보완

답글 버튼을 누른 뒤 댓글 입력창이 열려도 포커스가 버튼에 남아, 사용자가 입력창을 한 번 더 눌러야 했습니다. 댓글 작성과 답글 작성은 같은 입력창 상태를 쓰므로 두 진입 흐름을 함께 점검했습니다.

댓글 입력창 참조와 `requestAnimationFrame` 기반 포커스 효과를 추가했습니다. 이제 댓글 쓰기와 답글 버튼 모두 입력창이 렌더링된 직후 자동 포커스를 받습니다. 전용 경계 QA는 두 흐름의 실제 활성 요소를 검사하며, 전체 자동 검증은 **73개**, TypeScript 검사와 프로덕션 빌드는 모두 통과했습니다.

## 서비스 내 확인 토스트 전환

질문·힌트·댓글 삭제, 기록 가져오기, 브라우저 기록 초기화에 브라우저 기본 `window.confirm`이 남아 있었습니다. 이 기본 팝업은 서비스의 시각 언어와 분리되고, 브라우저마다 표시 방식이 달라지는 P1 일관성 결함으로 분류했습니다.

서비스 안의 확인 토스트로 모두 전환했습니다. 파괴적 동작은 ‘취소’와 행동별 승인 버튼을 가진 토스트에서 명시적으로 승인한 뒤에만 실행됩니다. Escape는 확인을 취소하고 토스트로 취소 피드백을 남깁니다. 가져오기 QA에는 서비스 내 교체 확인과 브라우저 기본 대화상자 미노출 검증을 추가했습니다.

전체 자동 검증은 **78개**, TypeScript 검사와 프로덕션 빌드는 모두 통과했습니다.

## Cloudflare Workers·KV 전환

정적 React 자산과 `/api/*` 요청을 하나의 Workers 배포로 처리하도록 구성했습니다. 전용 `SKACK_OVERFLOW_KV` 네임스페이스에는 서비스 전체 질문 상태(`state:questions`)와 익명 UID별 차단·읽음 상태(`user:{UID}`)를 분리해 저장합니다.

로컬 Workers 환경에서 질문 상태 저장·조회와 사용자 상태 저장·조회를 확인했습니다. 유효한 질문 구조는 `200`, 손상된 질문 구조는 `400`으로 처리됐습니다. Workers 타입 검사, 기존 자동 검증 **78개**, React·정적 자산 빌드가 모두 통과했습니다.

## Cloudflare 배포 UI 자산 복구

배포본의 헤더 심볼과 파비콘은 Manus 전용 `/manus-storage/...` 경로를 사용하고 있었습니다. 이 경로는 Cloudflare 정적 자산 번들에 포함되지 않아 배포 환경에서 이미지 아이콘으로 대체됐고, 헤더의 로고 폭도 흔들렸습니다.

사용자 제공 심볼을 `client/public/skack-symbol.png`으로 포함하고, 헤더·파비콘 참조를 Workers 정적 자산 경로로 전환했습니다. 심볼 컨테이너의 폭과 높이를 고정해 로딩 시에도 헤더 정렬이 흔들리지 않도록 했습니다. Workers 타입 검사·React 타입 검사·정적 자산 빌드에서 해당 파일이 `dist/public/skack-symbol.png`으로 출력되는 것을 확인했습니다.

## Quiet Signal UI·UX 고도화

데스크톱·모바일 화면을 점검한 결과, 문제 스트림은 신호 타임라인으로 읽히지만 히어로·도구 모음·오른쪽 패널은 일반 게시판처럼 분리되어 보이고, 오른쪽 하단 여백은 정보가 없는 대시보드 영역처럼 보였습니다.

히어로와 필터 사이에 로그 메타 라벨을 연결하고, 질문 행에는 수평 신호 피드백과 키보드 포커스 윤곽을 추가했습니다. 오른쪽 패널은 `ARCHIVE MARGIN / LIVE` 표식, 세로 신호선, 상태·태그·기록 도구 구획, 갱신 메모를 갖는 아카이브 여백으로 재구성했습니다. 로고에는 작은 teal 연결 신호와 Signal Orange 끊김 표식을 결합했으며, 모바일에서는 이 장식을 숨겨 텍스트와 작성 행동을 우선했습니다.

데스크톱과 390px 모바일 화면에서 로고·히어로·신호 스트림·검색·필터·작성 행동의 가독성과 가로 넘침 여부를 확인했습니다. Workers 타입 검사, React 타입 검사, 기존 키보드 포커스·댓글·답글·데이터 이관을 포함한 자동 검증 **78개**, 정적 자산 빌드가 모두 통과했습니다.

## 한국어 UX 카피 전환

토스와 Apple 한국 공식 페이지의 원문을 확인해, 행동과 결과를 짧게 연결하고 버튼에는 짧은 동사형을 쓰는 흐름만 참고했습니다. 서비스에 맞게 문구를 새로 작성했으며, 원문을 복제하지 않았습니다. 참고 원문과 적용 경계는 `COPY_REFERENCE_NOTES.md`에 기록했습니다.

메인 화면의 `OBSERVED HINTS`, `HINT ARRIVED`, `ARCHIVE MARGIN` 같은 영어식 아카이브 표현을 제거했습니다. ‘막힌 장면을, 다음 관점으로’는 ‘막힌 순간을, 같이 풀어봐요’로, ‘도움 신호·힌트 도착·관점 선택’은 ‘답변 기다리는 글·답변 달린 글·해결된 글’로 정리했습니다. 검색·정렬·내 기록·질문 작성 문구도 사용자가 바로 이해할 수 있는 질문·답변 중심 말로 바꿨습니다.

## 카피·선택자 최종 동기화

상단 탐색과 본문 필터를 모두 ‘전체’, ‘답변 기다리는 글’, ‘답변 달린 글’, ‘내 질문’으로 맞췄습니다. 질문을 여는 모든 행동은 ‘질문 올리기’로, 상세 화면의 입력·수정·삭제·작성 행동은 ‘답변’으로 통일했습니다. 질문 작성 성공 안내, 예시 작성자명, 초기화 확인 문구, 상세 닫기 접근 가능한 이름도 같은 기준을 적용했습니다.

자동 검증도 화면 문구를 그대로 선택하도록 갱신했습니다. Workers 타입 검사와 React 타입 검사, 전체 Playwright 자동 검증 **78개**, Workers 정적 자산 빌드를 다시 실행해 모두 통과했습니다. 데스크톱 전체 화면도 확인했으며, 상단 탐색·히어로 집계·질문 스트림·오른쪽 상태 패널이 자연스러운 한국어 문구로 표시됩니다.
