# SKACK Overflow QA 보고서

> 이 보고서는 기존 누적 기록을 제거한 뒤, 현재 프로젝트 상태를 대상으로 수행하는 반복 QA의 근거와 결과만 기록합니다.

## 검증 범위

익명 UID, 질문·힌트·댓글·답글, 공감·관점 선택 권한, 필터·검색, 링크 복사, 데이터 가져오기·내보내기, 탭 동기화, 접근성 피드백, 워드마크·색상 역할을 검증합니다.

## 1차 QA: 기능 검증과 시각 결함

TypeScript 검사, 프로덕션 빌드, 익명 UID·질문·힌트·댓글·답글·권한·필터·데이터 이관·탭 동기화·접근성·브랜드 규칙을 포함한 자동 검증을 실행했습니다. **67개** 자동 검증과 TypeScript 검사, 프로덕션 빌드가 통과했습니다.

독립 시각 검토에서는 피드의 신호 타임라인은 명확하지만 히어로 오른쪽이 비어 있어, 첫 화면이 실제 학습 로그보다 정적 소개 영역처럼 읽힌다는 P1 정보 위계 결함을 확인했습니다.

### 보완

히어로에 최근 힌트 도착 질문 두 개를 실제 데이터로 표시하는 관찰 기록 패널을 추가했습니다. 각 기록은 단계·힌트 수·작성 시점을 보여주며, 클릭하면 해당 막힘 상세를 엽니다. 모바일에서는 핵심 제목·집계를 우선하도록 관찰 패널을 숨깁니다.

보완 뒤 전체 자동 검증 **67개**, TypeScript 검사, 프로덕션 빌드를 다시 실행해 통과했습니다.

## 2차 QA: 차단 상태와 히어로 관찰 기록

히어로 관찰 기록은 힌트가 도착한 질문을 표시했지만, 작성자를 차단한 뒤에도 해당 작성자의 기록을 계속 표시했습니다. 피드와 상세에서는 차단이 반영되는데 히어로에서만 남아 개인정보 노출 제어가 일관되지 않은 P1 결함으로 분류했습니다.

### 보완

관찰 기록의 후보 질문을 만들 때 차단한 작성자를 제외했습니다. 전용 브라우저 검증은 히어로 기록 노출, 해당 막힘 상세 진입, 작성자 차단 후 기록 즉시 숨김을 확인합니다.

전체 자동 검증은 **70개**, TypeScript 검사와 프로덕션 빌드는 모두 통과했습니다.

## 3차 QA: 작성 행동의 화면·보조기술 언어 일치

목록의 작성 버튼은 화면에는 ‘막힘 남기기’로 표시되지만 접근 가능한 이름은 ‘질문하기’였습니다. 화면 독자와 보조기술 사용자가 서로 다른 행동 명칭을 접하는 P2 언어 일관성 결함으로 분류했습니다.

### 보완

목록 작성 버튼의 접근 가능한 이름을 ‘막힘 남기기’로 맞췄습니다. 경계 QA에는 목록 버튼의 접근 가능한 이름이 화면 문구와 일치하는지 확인하는 검증을 추가했습니다.

전체 자동 검증은 **71개**, TypeScript 검사와 프로덕션 빌드는 모두 통과했습니다.

## 1차 절차 재적용과 최종 대조

1차 QA 절차를 다시 적용해 익명 UID, 질문·힌트·댓글·답글, 공감·관점 선택 권한, 필터·검색, 링크 복사 성공·실패 경로, 데이터 가져오기·내보내기, 탭 동기화, 히어로 관찰 기록, 차단 동기화, 접근 가능한 이름, 색상 역할을 한 번에 대조했습니다.

실패 시 즉시 중단하는 방식으로 **71개** 자동 검증, TypeScript 검사, 프로덕션 빌드를 실행해 모두 통과했습니다. 모바일 화면에서도 관찰 패널은 숨기고 신호 집계·막힘 로그를 우선해 가로 넘침 없이 표시되는 것을 확인했습니다.
